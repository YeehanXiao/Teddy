% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifFunction.R
\name{export_transcript}
\alias{export_transcript}
\title{Export transcript sequences}
\usage{
export_transcript(
  txid,
  GTF,
  genome,
  outdir = "transcript_fasta",
  write = TRUE,
  collapse = TRUE
)
}
\arguments{
\item{txid}{Character vector. Transcript IDs to export.}

\item{GTF}{A `GRanges` object containing exon-level transcript annotation,
such as the output of `extractGTF(type = "exon")`.}

\item{genome}{Genome sequence object accepted by `Biostrings::getSeq()`,
such as a `BSgenome` object or `FaFile`.}

\item{outdir}{Character. Output directory for FASTA files.
Default is `"transcript_fasta"`.}

\item{write}{Logical. If `TRUE`, write sequences to FASTA files and invisibly
return output file paths. If `FALSE`, return a `DNAStringSet`.}

\item{collapse}{Logical. If `TRUE`, concatenate exon sequences into one
spliced transcript sequence. If `FALSE`, return or write exon sequences
separately.}
}
\value{
If `write = TRUE`, invisibly returns output FASTA file paths.
If `write = FALSE`, returns a `DNAStringSet`.
}
\description{
Export spliced transcript sequences by retrieving exon sequences from an
exon-level GTF `GRanges` object. Exons can either be concatenated into full
transcript sequences or exported separately.
}
