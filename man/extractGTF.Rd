% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R, R/utils.R
\name{extractGTF}
\alias{extractGTF}
\title{Extract GTF information}
\usage{
extractGTF(combineSE, filter = 1, type = c("exon", "transcript", "both"))

extractGTF(combineSE, filter = 1, type = c("exon", "transcript", "both"))
}
\arguments{
\item{combineSE}{A `SummarizedExperiment` object containing GTF metadata in its `@metadata$gtf` slot
and FPKM values in one of its assays.}

\item{filter}{A numeric value specifying the minimum FPKM threshold for transcripts to be included in the output. 
Default is 1.}

\item{type}{A character string specifying the type of genetic elements to extract. 
Valid options are "exon", "transcript", or "both". Default is "exon".}
}
\value{
A filtered subset of the GTF metadata from the `combineSE` object, 
including only the specified types of genetic elements that meet the FPKM threshold.

A filtered subset of the GTF metadata from the `combineSE` object, 
including only the specified types of genetic elements that meet the FPKM threshold.
}
\description{
This function filters and extracts GTF information from a `SummarizedExperiment` object 
based on the specified element type (exon, transcript, or both) and an FPKM threshold.

This function filters and extracts GTF information from a `SummarizedExperiment` object 
based on the specified element type (exon, transcript, or both) and an FPKM threshold. 
It is designed to work with `SummarizedExperiment` objects containing GTF metadata and FPKM assay data.
}
\examples{
# Assuming `mySE` is a `SummarizedExperiment` object with the necessary structure
extractGTF(mySE, filter = 1, type = "exon")

}
